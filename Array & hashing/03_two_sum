class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int,int> mpp;
        for(int i=0;i<nums.size();i++){
            int x = nums[i];
            if(mpp.find(target-x) != mpp.end()){
                return {mpp[target-x],i};
            }
            mpp[x] = i;
        }
        return {};
    }
};
